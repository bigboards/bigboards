{
  "node_name": "{{ ansible_hostname }}",
  "interface": "eth0",
  "retry_join": [
{% for host in groups['host']|reject(string(ansible_hostname)) %}
    {{ hostvars[host]['ansible_hostname'] }}.hex"{% if not loop.last %},{% endif %}"
{% endfor %}

  ],
  "tags": {
        "hex-id": "{{ ansible_local.bb.hex.id }}",
        "hex-name": "{{ ansible_local.bb.hex.name }}",
        "role": "{{ ansible_local.bb.node.role }}"
  }
}