{
  "node_name": "{{ ansible_hostname }}",
  "interface": "eth0",
  "retry_join": [
    "{{ ansible_local.hex.general.name }}-n1"
  ],
  "tags": {
        "hex-id": "{{ ansible_local.hex.general.id }}",
        "hex-name": "{{ ansible_local.hex.general.name }}",
        "role": "{{ ansible_local.hex.node.role }}"
  }
}