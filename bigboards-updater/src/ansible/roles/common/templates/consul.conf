{
  "datacenter": "{{ ansible_local.bb.hex.name }}",
  "data_dir": "/data/consul",
  "log_level": "INFO",
{% if 'host-coordinators' in group_names %}
  "server": true,
{% endif %}
  "advertise_addr": "",
  "advertise_addr_wan": ""
  "addresses": {
    "dns": "{{ ansible_eth1.ipv4.address }}",
    "http": "{{ ansible_eth1.ipv4.address }}",
    "https": "{{ ansible_eth1.ipv4.address }}",
    "rpc": "{{ ansible_eth0.ipv4.address }}"
  }
}