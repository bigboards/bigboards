{
  "node_name": "{{ ansible_hostname }}"
  "discover": "{{ ansible_local.hex.id }}",
  "tags": {
        "hex": "{{ ansible_local.hex.id }}",
        "role": "{{ ansible_local.hex.node.role }}"
  },

  "event_handlers": [
    "/opt/serf/events.d/etc-hosts.handler"
  ]
}