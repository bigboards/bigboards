description     "Serf agent"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

script
  if [ -e /etc/default/serf ]
  then
    . /etc/default/serf
    echo "Sourcing config file."
  else
    echo "Config file not available."
  fi
  if [ -d $EVENTS_DIR ]
  then
    echo "Entering directory $EVENTS_DIR."
    cd $EVENTS_DIR
    for event in $( ls *."$EVENT_HANDLER_SUFFIX" ); do
      handler="-event-handler=$EVENTS_DIR/$event"
      OPTIONS="$OPTIONS $handler "
    done
  else
    echo "Directory not available: $EVENTS_DIR"
  fi

  exec $SERF_HOME/bin/serf agent $OPTIONS >> /var/log/serf.log
end script