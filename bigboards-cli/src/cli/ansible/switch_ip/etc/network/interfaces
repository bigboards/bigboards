auto lo
iface lo inet loopback

# internal network interface
auto eth0
iface eth0 inet static
    address {{ ip_prefix }}.{{ ansible_local.bb.node.sequence }}
    netmask 255.255.255.0

# external network interface
auto eth0:0
iface eth0:0 inet dhcp